def analyze_item_groups(df):
  groups = df['item_category'].unique()
  d = {}
  for group in groups:
	df_group = df[df['item_category'] == group]
	totals = ...	
	bcd_counts = ...
	efghik_counts = ...

	d[group] = (bcd_counts , efghik_counts )
  return d